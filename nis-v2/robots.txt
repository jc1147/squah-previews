# NOBODY IS SAFE - v2 STAGING TREE. This is a complete duplicate of the public site,
# assembled for review by assemble-v2.mjs. It is not the public site and must not be indexed.
# Note: when this tree is served from a project path rather than an origin root, a crawler
# reads the ORIGIN robots.txt and never sees this file. The per-page
# <meta name="robots" content="noindex,nofollow"> on every page of this tree is what actually
# holds; this file is the belt to that pair of braces.
User-agent: *
Disallow: /
